import React from 'react';
import { Navbar, Nav } from 'react-bootstrap';

export default () => (
  <Navbar>
    <Navbar.Header>
      <Navbar.Brand>
        <a href="#home">React Starter</a>
      </Navbar.Brand>
    </Navbar.Header>
    <Nav />
  </Navbar>
);
